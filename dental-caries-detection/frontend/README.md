# Frontend

Placeholder created by BE-1.1. The React 18 + TypeScript + Vite application is
bootstrapped in FE-1.4 and containerized in FE-1.5 (owner: Sukollapat).

Until `frontend/Dockerfile` exists, start the stack without this service:

```bash
docker compose up -d db pgadmin backend
```
